# License notes (anonymous review)

This artifact aggregates:

1. **Original experiment code** in `src/meg_rw/`, `src/training/`, `src/evaluation/`, `src/recbole_ext/`, and analysis/scripts added for this package.
2. **Third-party open-source dependencies** used at runtime:
   - [RecBole](https://github.com/RUCAIBox/RecBole) (recommender library; see RecBole license)
   - [PyTorch](https://pytorch.org/) (see PyTorch license)
   - NumPy, SciPy, Pandas, Matplotlib, PyYAML, pytest (respective licenses)

A single top-level LICENSE file is **not** asserted here during anonymous review because:

- RecBole / PyTorch licensing must be respected by downstream users;
- the submitting authors should confirm the release license for original code after acceptance.

**Manual action after acceptance:** add an explicit top-level `LICENSE` for original code (e.g., MIT/Apache-2.0) and retain third-party notices.

`CITATION.cff` is omitted during double-blind review to avoid author identification. Citation metadata will be added after the review process.
